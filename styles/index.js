import {
  StyleSheet
} from 'react-native';

export const styles = StyleSheet.create({
  container: {
   flex: 1,
   paddingTop: 22
  },
  gameListItem: {
    padding: 10,
    fontSize: 18,
    height: 44
  },
});
